<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:layout_height="match_parent"
    tools:context="com.v1.foundspecs.milzam.skripsiapp.Activity.UploadActivity">
    <ImageView
        android:layout_width="match_parent"
        android:background="@color/colorBlack"
        android:src="@drawable/navbackgroud"
        android:scaleType="fitCenter"
        android:id="@+id/imgUpload"
        android:layout_height="380dp" />
    <android.support.v7.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:cardPreventCornerOverlap="false"
        app:cardCornerRadius="2dp"
        app:cardUseCompatPadding="true">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">
            <EditText
                android:layout_width="match_parent"
                android:padding="16dp"
                android:id="@+id/etKetUpload"
                android:hint="Berikan Keterangan ...."
                android:textColor="@color/cardview_dark_background"
                android:inputType="textMultiLine"
                android:textColorHint="@color/cardview_dark_background"
                android:maxLines="8"
                android:layout_height="wrap_content"
                android:layout_toLeftOf="@+id/btnUpload"
                android:layout_toStartOf="@+id/btnUpload" />
            <Button
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom"
                android:textColor="@color/colorWhiteSolid"
                android:text="KIRIM"
                android:id="@+id/btnUpload"
                android:background="@color/appColor"/>
        </LinearLayout>
    </android.support.v7.widget.CardView>

</LinearLayout>
